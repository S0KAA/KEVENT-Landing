function displayDownloadError() {
  const downloadErrorSection = document.getElementsByClassName('download-error')
  downloadErrorSection[0].style.display = 'block'
}

export default displayDownloadError
